# MPFlow environment template. Скопируйте в .env. Никогда не коммитьте реальные секреты.
# Полный гайд по локальному запуску — в README.md.
#
# MPFlow работает только на PostgreSQL: DATABASE_URL обязателен (локально — docker compose up -d).
# На первом старте создаётся владелец без подтверждения почты (owner-setup).

NODE_ENV=development
HOST=0.0.0.0
PORT=3004

# Persistence
ACCOUNTING_PERSISTENCE=postgres
DATABASE_URL=postgresql://mpflow:mpflow_local@127.0.0.1:54322/mpflow

# Symmetric key for encrypted marketplace credentials.
# Production value must be long, random and backed up. Losing it makes stored channel credentials unreadable.
ACCOUNTING_ENCRYPTION_KEY=mpflow-local-dev-stable-key-change-in-production

# Auth
AUTH_REQUIRED=true
# Секрет better-auth (подпись сессионных cookie и токенов). Обязателен в production.
# Генерация: npx @better-auth/cli@latest secret
BETTER_AUTH_SECRET=
# Хост, на который ведёт ссылка подтверждения email. В dev это Vite (:5174),
# где живёт SPA. В production — публичный https-адрес приложения.
PUBLIC_APP_URL=http://127.0.0.1:5174
CORS_ORIGIN=http://127.0.0.1:5174
# Пусто = владельцем становится первый зарегистрировавшийся (удобно локально).
# Для публичного сервера задайте список разрешённых email через запятую.
ACCOUNTING_AUTH_BOOTSTRAP_EMAILS=
ACCOUNTING_AUTH_PUBLIC_SIGNUP=true
ACCOUNTING_SAAS_WORKSPACES_ENABLED=true

# Email verification.
# Владелец (первый аккаунт) создаётся без подтверждения почты. Подтверждение нужно
# только следующим пользователям. SMTP не настроен → в dev ссылка печатается в
# консоль backend ([auth:mail] ...). Для production раскомментируйте блок ниже.
ACCOUNTING_AUTH_EMAIL_FROM="MPFlow <noreply@example.com>"
# ACCOUNTING_EMAIL_PROVIDER=smtp
# ACCOUNTING_AUTH_SMTP_HOST=smtp.example.com
# ACCOUNTING_AUTH_SMTP_PORT=465
# ACCOUNTING_AUTH_SMTP_SECURE=true
# ACCOUNTING_AUTH_SMTP_IGNORE_TLS=false
# ACCOUNTING_AUTH_SMTP_USER=noreply@example.com
# ACCOUNTING_AUTH_SMTP_PASS=

# Error tracking
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_RELEASE=local
SENTRY_TRACES_SAMPLE_RATE=0
VITE_SENTRY_TRACES_SAMPLE_RATE=0

# Prometheus /metrics. Required in production.
METRICS_TOKEN=local-metrics-token-change-in-production

# Optional integration defaults for local smoke tests.
# OZON_CLIENT_ID=
# OZON_API_KEY=

# S3-compatible object storage for product card media (photo studio).
# Timeweb Cloud example shown; any S3-compatible provider works.
S3_ENDPOINT=https://s3.twcstorage.ru
S3_REGION=ru-1
S3_BUCKET=mpflow
S3_ACCESS_KEY=
S3_SECRET_KEY=
# Public base for objects (Ozon downloads pictures by URL at export time).
S3_PUBLIC_BASE_URL=https://s3.twcstorage.ru/mpflow
S3_MEDIA_PREFIX=media
