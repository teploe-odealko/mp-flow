# ============================================================
# MPFlow Configuration
# ============================================================
# Copy this file to .env and customize values.
# Required values have defaults that work for local development.
# Optional values enable additional integrations.

# === Database ===
POSTGRES_PASSWORD=mpflow_dev_2026

# === Security ===
# Generate with: openssl rand -base64 32
HMAC_SECRET=dev-hmac-secret-change-in-production

# === Public URL of the proxy (used for MCP OAuth endpoints) ===
# BASE_URL=https://proxy.example.com

# === Admin Account (created on first startup) ===
# ⚠️  Change these before exposing to the internet!
ADMIN_BOOTSTRAP_USERNAME=admin
ADMIN_BOOTSTRAP_PASSWORD=change-me

# === Optional: Ozon Seller API ===
# Get credentials at https://seller.ozon.ru/app/settings/api-keys
# OZON_CLIENT_ID=
# OZON_API_KEY=

# === Optional: 1688 Supplier Enrichment (ali1688 plugin) ===
# Get token at https://tmapi.top
# TMAPI_API_TOKEN=

# === Optional: AI Features ===
# ANTHROPIC_API_KEY=

# === Optional: Logto OIDC Auth ===
# Without these, admin-ui uses simple username/password login.
# LOGTO_ENDPOINT=https://auth.example.com
# LOGTO_API_RESOURCE=https://proxy.example.com/api
# LOGTO_MCP_CLIENT_ID=
